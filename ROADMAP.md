# ai-calendar-assistant 剩余待办分期路线图与演进规划 (v1.20+ Roadmap)

> **版本定位**：基于 v1.19.1 生产基线，梳理剩余 1 项 P1 运维待办与 5 项 P3 演进待办的分期落地规划。  
> **制定日期**：2026-09-06  
> **制定角色**：Product Strategist（产品战略师）  
> **输入依据**：t1 待办背景与详细需求分析报告、t2 架构改造与技术依赖拆解报告、当前代码库实现与 andnode 生产运行现状。

---

## 一、战略背景与目标

### 1.1 现状与基线成果
截至 v1.19.1，系统已完成核心架构与基础交互的加固：
- **生产稳定性**：Docker 容器健康检查正常，AI 主模型与 CalDAV 只读探针 100% 成功，系统时区与版本指纹机制完备。
- **管理体验**：初次配置向导（4 步流程）、多主题支持（浅色/深色/跟随系统）、失败收件箱（带 UID 幂等防重）均已上线。
- **遗留待办**：`TODO.md` 中仅剩 1 项 P1 级实际运维端到端验收，以及 5 项 P3 级关于“交互安全性、操作可逆性、数据清晰度与可访问性”的深度演进需求。

### 1.2 演进战略定位
ai-calendar-assistant 的定位始终是 **“专注于自然语言理解与精准日程写入的轻量智能助手”**。  
下一阶段（Phase 3）的核心演进主线是：  
**从“能录入、能跑通”跨越到“改前有对比确认、批量有细粒度明细、改后有限时撤销与乐观锁防覆盖、Web 有只读看板与无障碍体验”的工业级可靠系统。**

---

## 二、剩余待办综合评估矩阵

结合业务价值（User Value）、风险防范度（Risk Mitigation）、工程复杂度（Engineering Cost）及前置依赖，对剩余 6 项待办进行四维评估：

| 序号 | 待办名称 | 原定优先级 | 用户与业务价值 | 工程实现复杂度 | 潜在风险 | 推荐实施阶段 |
|:---|:---|:---:|:---|:---:|:---|:---:|
| **1** | **实际日程写入、重新扫码与备份恢复验收** | P1 | **极高**<br>验证全链路生死线，消灭生产未知盲区 | **中**<br>约 1.0~1.5 人天 | 涉及生产真实写入与恢复，需严格备份与探针清理 | **Phase 3.1**<br>(立即执行) |
| **2** | **聊天修改前后对照与歧义确认** | P3 | **高**<br>防误修改、透明展示变更字段，消除 AI 幻觉 | **中**<br>约 1.5~2.0 人天 | 多轮对话上下文丢失、歧义匹配算法过于死板 | **Phase 3.2** |
| **3** | **限时撤销与远端 ETag 防覆盖校验** | P3 | **极高**<br>提供“反悔药”，乐观锁避免覆盖外部日历编辑 | **高**<br>约 1.5~2.0 人天 | 异构 CalDAV 对 ETag 支持差异，快照过期竞态 | **Phase 3.2** |
| **4** | **批量日程反馈与局部防重重试** | P3 | **高**<br>复杂文本多日程场景体验跃升，支持失败项单点重试 | **中**<br>约 1.0~1.5 人天 | 重试生成新 UID 导致重复建日程，需严格确定性 Hash | **Phase 3.2** |
| **5** | **轻量只读 CalDAV 日程列表** | P3 | **中高**<br>Web 控制台核对日程，闭环“录入-展示”，无需跳出 | **中**<br>约 2.0~2.5 人天 | 跨月范围查询性能、重复事件（RRULE）膨胀 | **Phase 3.3** |
| **6** | **视觉与可访问性统一 (WCAG AA)** | P3 | **中**<br>规范焦点、对比度、44px 触控靶心与读屏，提升专业度 | **低~中**<br>约 1.0~1.5 人天 | 样式回归影响移动端排版 | **Phase 3.3** |

---

## 三、分期演进全景路线图 (Phased Roadmap)

```
========================================================================================
[Phase 3.1: 运维基石闭环] (1.0~1.5d)  -->  目标：闭环 P1，消灭生产盲区
  ├─ 实测真实日历写入与自动清理验证
  ├─ 微信 Token 失效重新扫码与恢复测试
  └─ SQLite 数据冷备与一键还原实测
----------------------------------------------------------------------------------------
[Phase 3.2: 交互韧性与安全防护] (4.0~5.5d) --> 目标：数据变更“可确认、可回滚、防并发”
  ├─ 3.2a: 批量日程明细反馈与局部确定性防重重试 (P3-3)
  ├─ 3.2b: 聊天修改字段 Diff 对照与多候选歧义确认 (P3-1)
  └─ 3.2c: 限时撤销 (UndoToken 快照) 与 CalDAV ETag 乐观锁 (P3-2)
----------------------------------------------------------------------------------------
[Phase 3.3: 控制台体验与包容性设计] (3.0~4.0d) --> 目标：轻量可视、专业合规
  ├─ 3.3a: 轻量只读 CalDAV 列表面板（范围查询、时区格式化、只读边界） (P3-4)
  └─ 3.3b: WCAG AA 视觉对比度、焦点环、ARIA 标签与 44px 触控优化 (P3-5)
========================================================================================
```

### 3.1 Phase 3.1：运维基石与生产验收闭环 (v1.19.2)
- **目标**：彻底消除 `TODO.md` 中唯一的 P1 未结项，完成生产环境（andnode）真实业务端到端验证。
- **涉及待办**：实际日程写入、重新扫码与备份恢复验收。
- **核心落地动作**：
  1. **真实日程端到端写入与清理实测**：
     - 在已绑定的 iCloud 生产日历中，由 AI 写入一条带特有标记的真实测试日程（如 `[验证] Phase3 运维闭环验证`）。
     - 验证 CalDAV 成功返回、Web 记录正常展示、自动触发/手动触发探针清理。
  2. **微信重连与扫码容灾验证**：
     - 触发微信凭据主动失效测试，确认 Web 控制台提示、重新扫码流程以及凭据恢复后自动恢复消息监听。
  3. **备份恢复实操验证**：
     - 执行 `scripts/backup.sh` 导出 SQLite 与配置快照，并在测试路径模拟灾难恢复，验证数据库表结构完整性与记录一致性。
- **交付物**：P1 验收记录报告、`TODO.md` 勾选更新、备份恢复 SOP 手册校验。

---

### 3.2 Phase 3.2：会话交互韧性与数据安全体系 (v1.20.0)
- **目标**：解决“修改日程易改错、批量日程易重入、手滑误删不可挽回、外部并发修改易被覆盖”的核心痛点。
- **涉及待办**：
  - P3-1（修改前后对照与歧义确认）
  - P3-2（限时撤销与 ETag 校验防覆盖）
  - P3-3（批量日程反馈与局部防重重试）
- **协同设计与技术攻坚点**：
  1. **修改前后对照与歧义阻断 (P3-1)**：
     - **Diff 可视化**：对变更日程（时间、地点、标题、参与人）生成明确的「修改前 vs 修改后」对比高亮文本。
     - **歧义安全拦截**：当查询到 2 个以上同名或相似日程时，严禁 AI 默认猜测修改第一个，必须列出序号清单让用户回复确认。
  2. **限时撤销与 ETag 乐观并发锁 (P3-2)**：
     - **撤销快照存储**：新建 `undo_records` 表，记录被修改/被删除事件的原始 iCalendar VEVENT 文本、UID、ETag 及过期时间（默认 10 分钟 TTL）。
     - **用户交互**：操作成功后提示文案携带撤销指引（如：“已为您将会议调整至 15:00。10 分钟内回复「撤销」可恢复原状”）。
     - **ETag / If-Match 乐观锁**：回写前先读取远端当前 ETag，若与操作前不一致（说明用户已在手机日历修改过），则阻断覆盖并提示冲突。
  3. **批量日程细粒度反馈与幂等重试 (P3-3)**：
     - **结构化明细报告**：单次处理提取多条日程时，返回分项报告（如：`共识别 3 项：2 项写入成功，1 项因时间格式冲突失败`）。
     - **确定性哈希防重**：重试失败项时，基于内容哈希生成稳定的 UID，避免再次点击重试导致前序成功项被重复创建。
- **交付物**：高韧性消息处理器模块、撤销与 ETag 乐观锁服务、批量测试用例集（覆盖并发与重试场景）。

---

### 3.3 Phase 3.3：Web 控制台体验闭环与包容性设计 (v1.21.0)
- **目标**：在不破坏轻量架构的前提下，补齐控制台的日程感知能力与全站无障碍访问规范。
- **涉及待办**：
  - P3-4（轻量只读 CalDAV 日程列表）
  - P3-5（视觉和可访问性统一）
- **边界控制与设计规范**：
  1. **克制的高性能只读日程列表 (P3-4)**：
     - **严格只读**：明确界定 Web 端不开发沉重的月历视图、不提供拖拽编辑，避免与专业日历客户端同质化。
     - **范围查询与时区格式化**：支持按周（前后 7 天）或按月拉取 CalDAV 事件，前端显示更新时间戳、来源日历名称与时区信息。
     - **轻量缓存机制**：后端短时缓存（60 秒），支持显式“立即刷新”，防止对上游 CalDAV 服务频繁打满请求。
  2. **WCAG 2.1 AA 级无障碍与视觉优化 (P3-5)**：
     - **对比度达标**：修复深色模式下灰色副文本（从 3.2:1 提升至 >4.5:1）。
     - **触控与焦点**：所有可点击控件、按钮、标签保证移动端 ≥ 44×44px 触控区域；全局优化键盘导航 `:focus-visible` 环。
     - **读屏支持**：所有图标按钮补充清晰的 `aria-label`；动态加载状态补充 `aria-live="polite"`。
     - **缩放适配**：在 200% 页面缩放场景下无文字重叠与横向滚动溢出。
- **交付物**：Web 控制台只读日程组件、全站无障碍与视觉样式升级、A11y 自动化测试报告。

---

## 四、核心权衡与边界控制决策 (Architecture & Product Guardrails)

1. **坚持“聊天为主，Web 为辅”的产品边界**：
   - 绝不在 Web 控制台重造复杂日历套件。Web 控制台的定位始终是：**运维观测、配置引导、故障诊断、快捷核对**。
2. **限时撤销的“克制窗口”**：
   - 撤销窗口严格限定为 10 分钟。超过 10 分钟后快照自动失效，防范长周期数据漂移导致的逻辑混乱。
3. **ETag 乐观锁的向下兼容**：
   - 针对部分非标准 CalDAV 服务端（不返回 ETag 或 ETag 不变的问题），系统实施双重校验：优先匹配 ETag；若缺失则比对 `LAST-MODIFIED` 时间戳与内容 SHA256 摘要。
4. **批量处理的原子性权衡**：
   - 采用“尽力而为（Best-effort）+ 独立子项跟踪”而非“全量回滚事务”。因为现实生活中 5 个会议如果 4 个成功 1 个失败，全量回滚体验更差；清晰的失败明细与精准单项重试是最佳产品解。

---

## 五、下一步推进计划与具体行动建议 (Immediate Action Items)

| 阶段 | 周期 | 核心责任事项 | 验收标准 (DoD) |
|:---|:---:|:---|:---|
| **Sprint 1**<br>(即刻启动) | 1~2 天 | **Phase 3.1 运维验收实操**<br>1. 生产环境实测 CalDAV 真实写入与自动清理<br>2. 模拟微信 Token 失效与重新扫码恢复<br>3. 演练 `backup.sh` 备份恢复一致性 | 1. 真实日程在手机日历成功出现并被安全清理<br>2. 微信断连有报警且扫码即刻恢复<br>3. 数据库备份能 100% 完整还原，无记录丢失<br>4. 更新并勾选 `TODO.md` P1 项 |
| **Sprint 2** | 3~4 天 | **Phase 3.2a & 3.2b 交互防护**<br>1. 改造 `MessageProcessor` 支持 Diff 对照与歧义阻断<br>2. 实现批量日程解析报告与确定性 UID 防重生成器 | 1. 单元测试覆盖多歧义日程场景，AI 严禁乱猜<br>2. 批量 5 项任务部分失败时，重试仅写入失败项<br>3. pytest 增量测试 100% 通过 |
| **Sprint 3** | 2~3 天 | **Phase 3.2c 限时撤销与并发防护**<br>1. 增加 SQLite 撤销快照表与 TTL 清理机制<br>2. CalDAV If-Match / ETag 乐观锁改造与降级逻辑 | 1. 回复“撤销”能在 10 分钟内精准恢复旧日程<br>2. 远端并发修改被正确识别并阻断覆盖<br>3. 生产发布 v1.20.0 |
| **Sprint 4** | 3~4 天 | **Phase 3.3 控制台与可访问性**<br>1. CalDAV 只读列表接口与 Web 前端卡片开发<br>2. WCAG AA 视觉、对比度、键盘焦点与触控适配 | 1. Web 页面流畅只读展示前后 7 天日程<br>2. Chrome Lighthouse A11y 评分 ≥ 95 分<br>3. 生产发布 v1.21.0 |

---

## 六、总结与团队协作建议

通过将剩余 6 项待办梳理为 **Phase 3.1（运维闭环） -> Phase 3.2（交互与安全体系） -> Phase 3.3（Web 体验与无障碍）** 的阶梯序列，我们不仅实现了清晰的风险隔离与技术解耦，更让系统从基础运维底线向高可靠性、高可用性产品坚实跃升。建议团队立即启动 Phase 3.1 验收工作，夯实生产基石！
