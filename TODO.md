# 产品与 UI 优化待办

更新：2026-09-06。依据本次 v1.17.2 产品审查。前五轮改动已发布为 v1.18.0 并部署至 andnode。后续首屏 KPI 外露与去重优化已发布为 v1.18.1 并完成 andnode 生产部署与服务端/UI 验收。

## 已完成（本地）

- [x] 事件记录手机卡片化，优先展示标题、状态、时间，键盘展开详情。
- [x] 历史失败记录中文分类与处理建议，技术详情折叠。
- [x] 原生中文状态筛选、搜索标签、清除筛选和空结果状态。

## P1 · 先修复行为与信息可信度

- [x] CalDAV 测试、拉取、保存边界统一。验收：探测不落库，不丢草稿；保存才生效，失败保留输入；换账号不复用旧密码。
- [x] 概览统计与状态说明。验收：零样本不显示 100%；待处理不计成功；按配置时区统计今日；说明配置状态与本地日程统计范围。
- [x] 发布 v1.18.0 并完成 andnode 服务端验收：健康状态、文件哈希、数据库、真实 AI 与 CalDAV 连接通过；保留 v1.17.2 回滚镜像和升级前备份。
- [x] 线上浏览器验收：连接验证、分页筛选、手机详情、日历草稿不落库、微信在线与系统安全组件加载通过。
- [ ] 实际日程写入、重新扫码与备份恢复验收。

## P2 · 完善主要使用流程

- [x] 连接状态验证记录。概览显式验证已保存主模型/日历连接，记录结果与时间，配置变化及超过 24 小时需复核；最近日历业务成功仅作为历史参考。
- [x] 业务成功与配置版本关联。为 AI/日历业务链记录实际使用配置版本及成功时间，避免把旧配置的成功归于当前配置。
- [x] 概览层级重排。缺失配置和今日失败提示、最近处理活动前置；连接压缩为摘要，统计可折叠。历史失败不等同于未解决问题。
- [x] 初次配置引导。AI → 日历 → 任选聊天渠道 → 首条日程验证，其他渠道可跳过。
- [x] 失败收件箱。显示发生阶段、原因和下一步，提供回聊天补充/复制原文；重试写入先设计事件防重和 Web 操作边界。
- [x] 微信恢复入口。在线时折叠扫码与诊断字段；登录失效时突出重新扫码；账号替换与清除凭据放次级区域。
- [x] 系统设置分组。账户与安全、日程偏好、数据与备份分开；保存按钮具名，补备份恢复说明和验证指引。
- [x] 登录体验。通行密钥取消、超时、不支持分别给中文提示；登录按钮主次明确，首次部署帮助折叠。
- [x] 记录分页与更多筛选。支持查看超过 100 条的历史，增加渠道/日期筛选，区分处理时间与日程时间。
- [x] 配置能力验收。区分模型连接、有效日程结构、识图能力、日历连接与写入权限；实际写入测试明确影响及清理结果。

## P3 · 需求确认后迭代

- [ ] 聊天修改前后对照与歧义确认。先明确目标，再展示字段变化；删除歧义不默认猜测。
- [ ] 限时撤销。保存恢复快照并核对远端最新状态，避免覆盖之后的修改。
- [ ] 批量日程反馈。保留已有多日程能力，逐项展示成功/失败；重试只处理失败项且防重。
- [ ] 轻量只读日程列表。读取目标 CalDAV 实时结果，注明来源、更新时间与时区；复杂月历/拖拽编辑后置。
- [ ] 视觉和可访问性统一。改善暗色小字对比度、图标、间距、按钮与焦点；完成读屏、200% 缩放及触控验证。
- [x] 日历高级配置细化。可搜索时区、服务商预设与帮助说明；保持自定义 CalDAV 兼容。

## 验证记录

首轮事件页：308 项 pytest 通过，390×844 与 1440×1000 演示数据浏览器验证通过。后续完成项目时补充验证并勾选对应项。

第二轮：313 项 pytest 通过。浏览器验证日历探测保留草稿、选择后保存及刷新回读；390 像素单列规则与桌面零样本状态已检查。使用模拟 CalDAV，未验证真实远端服务，未部署。

第三轮（2026-09-06）：316 项 pytest 通过，概览展示最近 5 条真实处理活动（含失败与删除），统一概览/记录页配置时区。已验证手机卡片、键盘折叠、统计展开和失败筛选入口；未部署。

第四轮（2026-09-06）：319 项 pytest 通过。记录按每页 25 条分页，渠道/处理日期筛选跨页保留；日程偏好独立保存，不触发历史清理；微信凭据失效突出扫码，诊断及清除凭据折叠。浏览器使用演示状态验证，真实扫码、备份恢复和线上验收仍未执行。

第五轮（2026-09-06）：323 项 pytest 通过。保存配置的显式验证记录与草稿探测分离；模拟日历测试、刷新保留结果、未配置主模型反馈及手机布局已验证。未部署，未测试真实远端服务。

Phase 1 优化与全量回归（2026-09-06）：342 项 pytest 100% 通过（新增 19 项针对配置版本指纹、EventRecord 增量迁移、AI Schema 探针、CalDAV 写入权限探针与自动回滚清理、日历主流服务商预设与 IANA 时区、通行密钥登录体验优化与异常友好提示）。git diff --check 0 警告，静态资源与版本哈希一致性验证通过。

Phase 2 优化与全量回归（2026-09-06）：364 项 pytest 100% 通过（新增 22 项针对失败收件箱阶段打标与 EventRecord 迁移回填、Web 重试并发锁、非法重试拦截与 UID 幂等防重机制、初次配置向导 4 步流程与首条测试日程端到端接口、亮暗色多主题 CSS 变量与 HTML 属性规范）。git diff --check 0 警告通过。

发布验收（2026-09-06）：代码提交 85af3e3，发布提交 7cafa0b，Release run 34003173641 成功。生产 v1.18.0 healthy、0 重启；关键文件哈希一致，数据库检查通过，内部健康/静态资源/登录跳转正常，真实 AI 与 CalDAV 连接通过。未进行线上浏览器或实际日程写入验证。

v1.19.0 发布与生产验收（2026-09-06）：代码提交 c1bfcd2，发布提交 edf4310，Release run 34029164564 自动化生成 tag v1.19.0，Docker 镜像 ghcr.io/j2st1n/ai-calendar-assistant:v1.19.0 构建发布。andnode 生产环境灾备与升级切换完成（备份 pre-v1.19.0-20260906、保留 v1.18.1 回滚镜像 36572d753c8f）。线上生产环境全量验收通过：
1) 容器 healthy，0 重启，启动与运行日志无 traceback；
2) 内部 (http://127.0.0.1:9527/health) 与公网 (https://cal.3313107.xyz/health) 均返回 HTTP 200 及 {"status":"ok","version":"v1.19.0"}；
3) AI 模型（gemini-3.8-flash-high via OpenAI 兼容接口）与 CalDAV（iCloud 日历服务，探测发现 5 个日历）只读安全探测 100% 成功通过；
4) Web 控制台首屏、向导路由 (/console/wizard 四步流程) 与多主题支持（跟随系统/浅色/深色切换及 CSS 变量）验证通过；
5) 本地分支代码与远端 Release v1.19.0 保持完全同步。
