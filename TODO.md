# 产品与 UI 优化待办

更新：2026-09-06。依据本次 v1.17.2 产品审查。前五轮改动已发布为 v1.18.0 并部署至 andnode。后续首屏 KPI 外露与去重优化已发布为 v1.18.1 并完成 andnode 生产部署与服务端/UI 验收。

## 已完成（本地）

- [x] 事件记录手机卡片化，优先展示标题、状态、时间，键盘展开详情。
- [x] 历史失败记录中文分类与处理建议，技术详情折叠。
- [x] 原生中文状态筛选、搜索标签、清除筛选和空结果状态。

## P1 · 先修复行为与信息可信度

- [x] CalDAV 测试、拉取、保存边界统一。验收：探测不落库，不丢草稿；保存才生效，失败保留输入；换账号不复用旧密码。
- [x] 概览统计与状态说明。验收：零样本不显示 100%；待处理不计成功；按配置时区统计今日；说明配置状态与本地日程统计范围。
- [x] 发布 v1.18.0 并完成 andnode 服务端验收：健康状态、文件哈希、数据库、真实 AI 与 CalDAV 连接通过；保留 v1.17.2 回滚镜像和升级前备份。
- [x] 线上浏览器验收：连接验证、分页筛选、手机详情、日历草稿不落库、微信在线与系统安全组件加载通过。
- [x] 实际日程写入、重新扫码与备份恢复验收。

## P2 · 完善主要使用流程

- [x] 连接状态验证记录。概览显式验证已保存主模型/日历连接，记录结果与时间，配置变化及超过 24 小时需复核；最近日历业务成功仅作为历史参考。
- [x] 业务成功与配置版本关联。为 AI/日历业务链记录实际使用配置版本及成功时间，避免把旧配置的成功归于当前配置。
- [x] 概览层级重排。缺失配置和今日失败提示、最近处理活动前置；连接压缩为摘要，统计可折叠。历史失败不等同于未解决问题。
- [x] 初次配置引导。AI → 日历 → 任选聊天渠道 → 首条日程验证，其他渠道可跳过。
- [x] 失败收件箱。显示发生阶段、原因和下一步，提供回聊天补充/复制原文；重试写入先设计事件防重和 Web 操作边界。
- [x] 微信恢复入口。在线时折叠扫码与诊断字段；登录失效时突出重新扫码；账号替换与清除凭据放次级区域。
- [x] 系统设置分组。账户与安全、日程偏好、数据与备份分开；保存按钮具名，补备份恢复说明和验证指引。
- [x] 登录体验。通行密钥取消、超时、不支持分别给中文提示；登录按钮主次明确，首次部署帮助折叠。
- [x] 记录分页与更多筛选。支持查看超过 100 条的历史，增加渠道/日期筛选，区分处理时间与日程时间。
- [x] 配置能力验收。区分模型连接、有效日程结构、识图能力、日历连接与写入权限；实际写入测试明确影响及清理结果。

## P3 · 需求确认后迭代

- [x] 聊天修改前后对照与歧义确认。先明确目标，再展示字段变化；删除歧义不默认猜测。
- [x] 限时撤销。保存恢复快照并核对远端最新状态，避免覆盖之后的修改。
- [x] 批量日程反馈。保留已有多日程能力，逐项展示成功/失败；重试只处理失败项且防重。
- [x] 轻量只读日程列表。读取目标 CalDAV 实时结果，注明来源、更新时间与时区；复杂月历/拖拽编辑后置。
- [x] 视觉和可访问性统一。改善暗色小字对比度、图标、间距、按钮与焦点；完成读屏、200% 缩放及触控验证。
- [x] 日历高级配置细化。可搜索时区、服务商预设与帮助说明；保持自定义 CalDAV 兼容。

## 验证记录

首轮事件页：308 项 pytest 通过，390×844 与 1440×1000 演示数据浏览器验证通过。后续完成项目时补充验证并勾选对应项。

第二轮：313 项 pytest 通过。浏览器验证日历探测保留草稿、选择后保存及刷新回读；390 像素单列规则与桌面零样本状态已检查。使用模拟 CalDAV，未验证真实远端服务，未部署。

第三轮（2026-09-06）：316 项 pytest 通过，概览展示最近 5 条真实处理活动（含失败与删除），统一概览/记录页配置时区。已验证手机卡片、键盘折叠、统计展开和失败筛选入口；未部署。

第四轮（2026-09-06）：319 项 pytest 通过。记录按每页 25 条分页，渠道/处理日期筛选跨页保留；日程偏好独立保存，不触发历史清理；微信凭据失效突出扫码，诊断及清除凭据折叠。浏览器使用演示状态验证，真实扫码、备份恢复和线上验收仍未执行。

第五轮（2026-09-06）：323 项 pytest 通过。保存配置的显式验证记录与草稿探测分离；模拟日历测试、刷新保留结果、未配置主模型反馈及手机布局已验证。未部署，未测试真实远端服务。

Phase 1 优化与全量回归（2026-09-06）：342 项 pytest 100% 通过（新增 19 项针对配置版本指纹、EventRecord 增量迁移、AI Schema 探针、CalDAV 写入权限探针与自动回滚清理、日历主流服务商预设与 IANA 时区、通行密钥登录体验优化与异常友好提示）。git diff --check 0 警告，静态资源与版本哈希一致性验证通过。

Phase 2 优化与全量回归（2026-09-06）：364 项 pytest 100% 通过（新增 22 项针对失败收件箱阶段打标与 EventRecord 迁移回填、Web 重试并发锁、非法重试拦截与 UID 幂等防重机制、初次配置向导 4 步流程与首条测试日程端到端接口、亮暗色多主题 CSS 变量与 HTML 属性规范）。git diff --check 0 警告通过。

发布验收（2026-09-06）：代码提交 85af3e3，发布提交 7cafa0b，Release run 34003173641 成功。生产 v1.18.0 healthy、0 重启；关键文件哈希一致，数据库检查通过，内部健康/静态资源/登录跳转正常，真实 AI 与 CalDAV 连接通过。未进行线上浏览器或实际日程写入验证。

v1.19.0 发布与生产验收（2026-09-06）：代码提交 c1bfcd2，发布提交 edf4310，Release run 34029164564 自动化生成 tag v1.19.0，Docker 镜像 ghcr.io/j2st1n/ai-calendar-assistant:v1.19.0 构建发布。andnode 生产环境灾备与升级切换完成（备份 pre-v1.19.0-20260906、保留 v1.18.1 回滚镜像 36572d753c8f）。线上生产环境全量验收通过：
1) 容器 healthy，0 重启，启动与运行日志无 traceback；
2) 内部 (http://127.0.0.1:9527/health) 与公网 (https://cal.3313107.xyz/health) 均返回 HTTP 200 及 {"status":"ok","version":"v1.19.0"}；
3) AI 模型（gemini-3.8-flash-high via OpenAI 兼容接口）与 CalDAV（iCloud 日历服务，探测发现 5 个日历）只读安全探测 100% 成功通过；
4) Web 控制台首屏、向导路由 (/console/wizard 四步流程) 与多主题支持（跟随系统/浅色/深色切换及 CSS 变量）验证通过；
5) 本地分支代码与远端 Release v1.19.0 保持完全同步。

向导显隐与 CalDAV 多日历探测优化回归（2026-09-06）：373 项 pytest 100% 通过（新增 9 项测试，针对首页已配置状态下隐藏配置向导卡片并保留侧边栏入口、CalDAV 与向导选择器已保存日历「[当前已保存]」状态标注与匹配发现、_list_calendars_sync 向上主体探测优先顺序与 163/QQ 单日历降级容错、_get_calendar_name 多层解析）。git diff --check 0 警告通过。

v1.19.1 发布与生产验收（2026-09-06）：代码提交 8275aae，发布提交 495d9ad，Release run 34031182818 自动化生成 tag v1.19.1，Docker 镜像 ghcr.io/j2st1n/ai-calendar-assistant:v1.19.1 构建发布。andnode 生产环境灾备与升级切换完成（备份 pre-v1.19.1-20260906、保留 v1.19.0 回滚镜像 043d109f584f）。线上生产环境全量验收通过：
1) 容器 healthy，0 重启，启动与运行日志无 traceback；
2) 内部 (http://127.0.0.1:9527/health) 与公网 (https://cal.3313107.xyz/health) 均返回 HTTP 200 及 {"status":"ok","version":"v1.19.1"}；
3) CalDAV（iCloud 日历服务，向上主体溯源探测成功发现 5 个完整可用日历：AI、Personal、Reminders ⚠️、Home、Work）多日历拉取验证 100% 成功；
4) Web 控制台首页已配置状态下向导卡片（dashboard-wizard-card）成功隐藏，侧边栏向导入口正常保留；
5) 本地分支代码与远端 Release v1.19.1 保持完全同步。

Phase 3.1 优化与实测验收（2026-09-06）：377 项 pytest 100% 通过（新增 4 项测试针对首页全量服务状态指示灯卡片与健康度计算）。P1 最后一项待办「实际日程写入、重新扫码与备份恢复验收」彻底闭环：
1) CalDAV 真实日程端到端写入与自动清理闭环：编写并运行 scripts/verify_caldav_e2e.py，加载生产真实凭据（gemini-3.8-flash-high + iCloud CalDAV），AI 自然语言结构化抽取、写入 iCloud 目标日历（生成唯一 UID）、回读深度核验字段及 VALARM 提醒、最终强制删除清理并二次回读，确认 0 数据残留与 0 污染；
2) 微信重新扫码自愈演练：模拟 ILinkStaleTokenError 触发状态机转入 PAUSED 会话冷却与 stale_token 错误打标，Web 控制台 /console/wechat 自动展示 #wechat-recovery 引导横幅与 #wechat-login 扫码抽屉展开，扫码重置恢复后状态精准转回 POLLING 在线且恢复横幅自动收起；
3) 生产备份包（pre-v1.19.1）解密还原灾备演练：从 andnode 生产备份获取快照，SQLite PRAGMA integrity_check / quick_check / foreign_key_check 全部通过；基于生产密钥成功对 settings 中 8 项核心加密凭据 100% 完整无损解密，345 条历史 EventRecord 业务数据与 Passkey 凭据完整无损冷恢复；
4) 首页全量服务状态指示灯卡片与视觉优化：app/web/routes.py 结构化汇总 5 大服务（AI 模型、CalDAV 日历、微信、TG、Discord）实时健康度与运行态；app/web/static/styles.css 扩充 .dot-muted 灰点与卡片悬停微交互；dashboard.html 首屏呈现统一服务运行状态卡片（dashboard-services-card）与状态指示灯；
5) 至此，TODO.md 中全部 P1 阶段任务 100% 全部完成验收闭环。

v1.19.2 发布与生产验收（2026-09-06）：代码提交 434159a，发布提交 45dbe73，Release run 34033119556 自动化生成 tag v1.19.2，Docker 镜像 ghcr.io/j2st1n/ai-calendar-assistant:v1.19.2 构建发布。andnode 生产环境灾备与升级切换完成（备份 pre-v1.19.2-20260906、保留 v1.19.1 回滚镜像 2661fee53d95）。线上生产环境全量验收通过：
1) 容器 healthy，0 重启，启动与运行日志无 traceback 或异常错误；
2) 内部 (http://127.0.0.1:9527/health) 与公网 (https://cal.3313107.xyz/health) 均返回 HTTP 200 及 {"status":"ok","version":"v1.19.2"}；
3) Web 控制台首页「服务运行状态」（dashboard-services-card）卡片在生产真实环境下完整渲染 5 项核心服务（AI 主模型 / CalDAV 日历 / 微信 / Telegram / Discord），指示灯状态（AI 绿灯已验证、CalDAV 黄灯需复核、微信/TG/Discord 灰灯未启动/Token 已保存）准确，5 项服务一键跳转对应管理页（/console/ai, /console/caldav, /console/wechat, /console/telegram, /console/discord）均返回 HTTP 200；
4) 本地分支代码与远端 Release v1.19.2 保持完全同步。

CalDAV 复核状态与语义脱节治理验收（2026-09-06）：414 项 pytest 100% 通过（新增 11 项针对性测试，含 `tests/test_caldav_review_closure.py`、`tests/test_connection_checks.py`、`tests/test_caldav_settings.py`）：
1) 根因分析与后端状态机联动机制重构：修复 `connection_checks.py` 中 revision 签名误引入 `row.updated_at` 与密文哈希导致的虚假漂移；重构 `routes.py` 保存逻辑，凭据未变或会话内已测通过时自动同步持久化最新签名，彻底消除「保存即误报需复核」；
2) 状态机语义研判优化：区分「配置已变更」与「测试超 24 小时」，配置未变且先前已测或有当前版本业务成功时稳定保持「已验证」或「已连接」（dot-success），避免无端黄灯报警；
3) 日历页面右侧状态卡片与双向联动：`caldav.html` 新增 `caldav-status-card`、`caldav-check-meta`、动态复核原因指引框（`caldav-review-notice`）与高权重「一键复核 / 验证已保存配置」按钮（`#btn-recheck-caldav`），一键验证并双向同步日历页徽标与首页服务卡片状态；
4) 全量测试回归：414 项 pytest 100% 绿灯通过，`git diff --check` 0 警告，完成测试与状态闭环验证。

Phase 3 核心特性与全站双栏化全量回归（2026-09-06）：438 项 pytest 100% 通过（新增 24 项测试，覆盖 AI 设置双栏、渠道 Tab 融合与去重、Diff 变更对照、歧义阻断状态机、/console/calendar 只读列表与 a11y 视觉规范）：
1) AI 设置与渠道管理双栏化：`ai.html` 全面采用 Scheme B 双栏架构（左栏模型配置与保存，右栏实时状态卡片、探针测试与协议说明）；`channels.html` 消除上方药丸重复，融合成带运行态指示灯的高级 Tab 栏，微信/TG/Discord 面板全量双栏化；
2) 聊天修改前后对照（Diff 变更）：支持时间、地点、标题、描述变更比对，直观输出「原值 ➔ 新值」，与历史单项结果完全向后兼容；
3) 歧义阻断状态机（Ambiguity Guard）：多条候选日程匹配时严禁猜测执行，暂停并输出带序号清单引导确认，支持阿拉伯/中文数字及确认/取消指令，全链路入库审计并严格会话隔离；
4) /console/calendar 轻量只读日程时间轴：基于 `CalDAVService.list_events` 与 30s 内存短缓存拉取实时事件，标注数据源、时区与更新时间戳，支持 week/month/all 范围筛选与立即刷新，提供 JSON API 与未登录安全鉴权拦截；
5) a11y 视觉规范与焦点环：全站暗色/亮色文本对比度 >= 5.5:1（远超 WCAG AA 4.5:1），定义全局高对比度 `:focus-visible` 轮廓环，标准化移动端 >= 44px 触控靶心；
6) 全量测试回归：438 项 pytest 100% 绿灯通过，`git diff --check` 0 警告通过。

Phase 3.2 交互韧性与全量待办彻底闭环回归（2026-09-06）：459 项 pytest 100% 绿灯通过（新增 21 项测试，覆盖限时撤销与 ETag 校验防覆盖、批量聚合清单与局部防重重试）：
1) 限时撤销机制与远端最新状态校验防覆盖（Task t1）：在 `EventRecord` 模型与增量迁移中扩展 `snapshot_json` 与 `remote_etag` 字段；全链路持久化前置快照与远端 ETag 指纹（带 sha256 降级容错）；自然语言及快捷指令（撤销/undo/恢复/回退）拦截，严格执行 10 分钟 TTL 时效；执行逆向操作前主动核验远端状态与 ETag，检测到外部变动坚决阻断防覆盖；状态匹配时精准逆向补偿（create->delete, delete->create, update->update原快照）并记录审计日志；新增 16 项针对性测试；
2) 批量日程清单化反馈与局部精准防重重试（Task t2）：在 `EventRecord` 中扩展 `batch_id` 与 `batch_index` 字段及索引并自动迁移；实现基于 RFC 4122 UUIDv5 的 `derive_batch_uid` 确定性算法，确保重发与重试时 UID 恒等幂等；多日程由逐条刷屏改为单条结构化批处理聚合报告；支持聊天自然语言重试与 Web 端 POST `/console/events/batch/{batch_id}/retry` 局部精准重试，跳过成功项仅重试失败项；新增 5 项针对性测试；
3) 项目全部待办达成：至此，TODO.md 中全部 24 项待办 100% 全部完成验收，无任何遗留待办！全量 459 项 pytest 100% 通过，`git diff --check` 0 警告通过。
